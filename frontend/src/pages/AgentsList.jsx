export default function AgentsList({ openAgent }) {
  return (
    <main className="view">
      <section className="page-intro">
        <p className="eyebrow">Agents</p>
        <h2>All agents</h2>
        <p>List of all agents will be fetched from the backend in future iterations.</p>
      </section>
    </main>
  )
}
